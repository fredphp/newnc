// 开心农场 - 完全匹配原项目数据库结构
// 基于 https://github.com/fredphp/nongchang.git 的 sql/shujuku.sql

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

// ==================== 管理员表 ====================
model Admin {
  id        Int      @id @default(autoincrement())
  username  String   @unique 
  password  String   
  loginip   String   
  logintime Int
  lock      Int      @default(0)

  @@map("think_admin")
}

// ==================== 用户表 ====================
model User {
  id          Int       @id @default(autoincrement())
  username    String?   
  password    String    @default("") 
  nickname    String?   
  phone       String?   
  QQ          String?   
  weixin      String?   
  zfb         String?   
  realname    String?   
  idcard      String?   
  regtime     DateTime? 
  logtime     DateTime? 
  lognum      Int?
  logip       String?   
  RegIP       String?   
  status      Int?      @default(1)
  remark      String?   
  upxianid    Int?
  upxianuser  String?   
  UserEndsNum Int?
  
  // 关联
  userlist    Userlist?

  @@map("think_user")
}

// ==================== 用户游戏数据表 (核心表) ====================
model Userlist {
  ID            Int       @id @default(autoincrement())
  userid        Int       @unique
  username      String?   
  password      String?   
  
  // 基础属性
  zhongzi       Int       @default(0)    // 种子数量
  gold          String    @default("0")    // 金币
  rmb           String    @default("0")    // 人民币
  lvl           Int       @default(1)    // 等级
  zs            String    @default("0")    // 钻石
  dong          String    @default("0")    // 冻结
  
  // 12块土地状态 (-1=未开垦, 0=空地, 1-12=种植的作物ID)
  zt1           String    @default("-1") 
  zt2           String    @default("-1") 
  zt3           String    @default("-1") 
  zt4           String    @default("-1") 
  zt5           String    @default("-1") 
  zt6           String    @default("-1") 
  zt7           String    @default("-1") 
  zt8           String    @default("-1") 
  zt9           String    @default("-1") 
  zt10          String    @default("-1") 
  zt11          String    @default("-1") 
  zt12          String    @default("-1") 
  
  // 土地开垦状态 (0=未开垦, 1=已开垦)
  tudi1         Int       @default(0)
  tudi2         Int       @default(0)
  tudi3         Int       @default(0)
  tudi4         Int       @default(0)
  tudi5         Int       @default(0)
  tudi6         Int       @default(0)
  tudi7         Int       @default(0)
  tudi8         Int       @default(0)
  tudi9         Int       @default(0)
  tudi10        Int       @default(0)
  tudi11        Int       @default(0)
  tudi12        Int       @default(0)
  
  // 各块土地的开垦/种植时间
  kttime1       DateTime? 
  kttime2       DateTime? 
  kttime3       DateTime? 
  kttime4       DateTime? 
  kttime5       DateTime? 
  kttime6       DateTime? 
  kttime7       DateTime? 
  kttime8       DateTime? 
  kttime9       DateTime? 
  kttime10      DateTime? 
  kttime11      DateTime? 
  kttime12      DateTime? 
  
  // 土地果实数量
  land_gs1      String?   @default("0") 
  land_gs2      String?   @default("0") 
  land_gs3      String?   @default("0") 
  land_gs4      String?   @default("0") 
  land_gs5      String?   @default("0") 
  land_gs6      String?   
  land_gs7      String?   @default("0") 
  land_gs8      String?   @default("0") 
  land_gs9      String?   @default("0") 
  land_gs10     String?   @default("0") 
  land_gs11     String?   @default("0") 
  land_gs12     String?   @default("0") 
  
  // 种植次数
  zz_count1     String?   @default("0") 
  zz_count2     String?   @default("0") 
  zz_count3     String?   @default("0") 
  zz_count4     Int       @default(0)
  zz_count5     Int       @default(0)
  zz_count6     Int       @default(0)
  zz_count7     Int       @default(0)
  zz_count8     Int       @default(0)
  zz_count9     Int       @default(0)
  zz_count10    Int       @default(0)
  zz_count11    Int       @default(0)
  zz_count12    Int       @default(0)
  
  // 加速次数
  js1           Int       @default(0)
  js2           Int       @default(0)
  js3           Int       @default(0)
  js4           Int       @default(0)
  js5           Int       @default(0)
  js6           Int       @default(0)
  js7           Int       @default(0)
  js8           Int       @default(0)
  js9           Int       @default(0)
  js10          Int       @default(0)
  js11          Int       @default(0)
  js12          Int       @default(0)
  
  // 施肥次数
  sf1           Int       @default(0)
  sf2           Int       @default(0)
  sf3           Int       @default(0)
  sf4           Int       @default(0)
  sf5           Int       @default(0)
  sf6           Int       @default(0)
  sf7           Int       @default(0)
  sf8           Int       @default(0)
  sf9           Int       @default(0)
  sf10          Int       @default(0)
  sf11          Int       @default(0)
  sf12          Int       @default(0)
  
  // 除草次数
  sc1           Int       @default(0)
  sc2           Int       @default(0)
  sc3           Int       @default(0)
  sc4           Int       @default(0)
  sc5           Int       @default(0)
  sc6           Int       @default(0)
  sc7           Int       @default(0)
  sc8           Int       @default(0)
  sc9           Int       @default(0)
  sc10          Int       @default(0)
  sc11          Int       @default(0)
  sc12          Int       @default(0)
  
  // 除虫次数
  cc1           Int       @default(0)
  cc2           Int       @default(0)
  cc3           Int       @default(0)
  cc4           Int       @default(0)
  cc5           Int       @default(0)
  cc6           Int       @default(0)
  cc7           Int       @default(0)
  cc8           Int       @default(0)
  cc9           Int       @default(0)
  cc10          Int       @default(0)
  cc11          Int       @default(0)
  cc12          Int       @default(0)
  
  // 各种作物的收获数量
  hetao         String    @default("0")    // 核桃
  hongzao       String    @default("0")    // 红枣
  putao         String    @default("0")    // 葡萄
  hamigua       String    @default("0")    // 哈密瓜
  shamoguo      String    @default("0")    // 沙漠果
  shiliu        String    @default("0")    // 石榴
  xiangli       String    @default("0")    // 香梨
  rensheuguo    String    @default("0")    // 人参果 (原字段名 rensheuguo)
  xunyichao     String    @default("0")    // 薰衣草
  shamorenshen  String    @default("0")    // 沙漠人参
  badanmu       String    @default("0")    // 巴旦木
  hetianyu      String    @default("0")    // 和田玉
  
  // 预计收益果实
  yj_hetao      String?   
  yj_shiliu     String?   
  yj_hongzao    String?   
  yj_putao      String?   
  yj_hamigua    String?   
  yj_xiangli    String?   
  yj_shamoguo   String?   
  yj_rensheuguo String?   
  yj_xunyichao  String?   
  yj_shamorenshen String? 
  yj_badanmu    String?   
  yj_hetianyu   String?   
  
  // 道具
  chutou        String    @default("10")   // 锄头
  jinyan        String    @default("0")    // 金眼
  muban         Int       @default(0)    // 木板
  shitou        Int       @default(0)    // 石头
  gangchai      Int       @default(0)    // 钢材
  
  // 宝石
  lvbaoshi      Int       @default(0)    // 绿宝石
  shashuihu     Int       @default(0)    // 杀水壶
  yinbaoxiang   Int       @default(0)    // 银宝箱
  chuchaoji     Int       @default(0)    // 除草剂
  shichaosx     Int       @default(0)    // 虫草生长剂
  tuchongsx     Int       @default(0)    // 土虫生长剂
  yulusx        Int       @default(0)    // 雨露生长剂
  zhhibaoshi    Int       @default(0)    // 智慧宝石
  lanbaoshi     Int       @default(0)    // 蓝宝石
  huangbaoshi   Int       @default(0)    // 黄宝石
  fengshousx    Int       @default(0)    // 丰收生长剂
  
  // 背景解锁
  bg1           Int       @default(1)
  bg2           Int       @default(0)
  bg3           Int       @default(0)
  
  // 狗粮
  ptgouliang    String    @default("0")    // 普通狗粮
  gaojigouliang String    @default("0")    // 高级狗粮
  gongjigouliang String   @default("0")    // 公鸡狗粮
  fygouliang    String    @default("0")    // 飞雁狗粮
  sdgouliang    String    @default("0")    // 闪电狗粮
  xygouliang    String    @default("0")    // 喜羊羊狗粮
  smgouliang    String    @default("0")    // 三毛狗粮
  
  // 其他道具
  chuchongji    String    @default("0")    // 除虫剂
  tongbaox      String    @default("0")    // 铜宝箱
  yinbaox       String    @default("0")    // 银宝箱
  jinbaox       String    @default("0")    // 金宝箱
  zhibaoshi     String    @default("0")    // 智慧石
  dhzs          String    @default("0")    // 兑换钻石
  
  // 自动功能
  autoshou      Int       @default(0)    // 自动收获
  autoshou_kttime DateTime? 
  autobozong    Int       @default(0)    // 自动播种
  autobozong_kttime DateTime? 
  
  // 房屋
  fangwu        Int       @default(1)    // 房屋等级
  
  // 其他
  youjifei      Int       @default(0)    // 有机肥
  logtime       DateTime? 
  lognum        Int       @default(1)
  islogin       String    @default("1") 
  sign          Int       @default(0)
  sign_time     DateTime? 
  jypwd         String?     // 交易密码
  qq            String?   
  upxianname    String?   
  upxianid      Int       @default(0)
  upxianuser    String?   
  
  zfb           String    @default("0") 
  weixin        String    @default("0") 
  phone         String    @default("0") 
  
  pay_zt        Int       @default(1)
  status        Int       @default(1)
  
  // 生长剂开启时间
  shichaosx_kttime  DateTime? 
  tuchongsx_kttime  DateTime? 
  yulusx_kttime     DateTime? 
  fengshousx_kttime DateTime? 
  
  // 关联
  user          User      @relation(fields: [userid], references: [id])

  @@index([userid])
  @@map("think_userlist")
}

// ==================== 作物列表 ====================
model CropsList {
  id                Int      @id @default(autoincrement())
  crops_name        String?     // 作物名称
  crops_identifier  String?     // 标识符
  crops_land_count  Int?                        // 种植数量
  crops_harvest     String?     // 收获时间(小时)
  crops_probability String?     // 收获概率
  crops_seed        String?     // 种子阶段时间
  crops_sprout      String?     // 发芽阶段时间
  crops_grow        String?     // 生长阶段时间
  crops_open_price  String?     // 开垦价格
  name              String?     // 英文名

  @@map("think_crops_list")
}

// ==================== 用户背包 ====================
model Userbag {
  id        Int      @id @default(autoincrement())
  name      String?    // 物品名称
  num       Int?                        // 数量
  userid    Int?                        // 用户ID
  username  String?    // 用户名
  buy_time  DateTime?      // 购买时间
  dq_time   DateTime?      // 到期时间

  @@index([userid])
  @@map("think_userbag")
}

// ==================== 用户日志 ====================
model UserLog {
  id        Int       @id @default(autoincrement())
  userid    Int
  username  String?   
  time      Int                     // 记录时间戳
  record    String      // 记录内容
  score     Float     @default(0)       // 积分变动
  source    Int       @default(0)       // 来源: 0任务 1邀请

  @@index([userid])
  @@map("think_user_log")
}

// ==================== 游戏配置 ====================
model GameConfig {
  id                            Int      @id @default(autoincrement())
  config_till_price             Int?
  config_eradicate_price        Int?
  config_seed_count             Int?
  config_sign_in                Int?
  config_register_reward        Int?
  config_steal_count            Int?
  config_pest_time              String?   @default("0.1")
  config_pest_odds              Int      @default(20)
  config_pest_reduce            Int      @default(5)
  config_drought_time           String?   @default("0.1")
  config_drought_odds           Int      @default(20)
  config_drought_reduce         Int      @default(8)
  config_weed_time              Int      @default(5)
  config_weed_odds              Int      @default(5)
  config_weed_reduce            Int      @default(5)
  config_weed_time2             Int      @default(5)
  config_weed_odds2             Int      @default(5)
  config_weed_reduce2           Int      @default(5)
  config_weed_time3             Int      @default(5)
  config_copperhoe_seed         Int      @default(3)
  config_silverhoe_seed         Int      @default(8)
  config_bumper_count           Int      @default(5)
  config_reborn_odds            Int      @default(20)
  config_reborn_cost            Int      @default(10)
  config_give_cost              Int      @default(10)
  config_gem_probability        Int      @default(10)
  config_gem_num                Int      @default(1)
  config_present_max            Int      @default(500)
  config_copper_goldbox         Int      @default(5)
  config_copper_silverbox       Int      @default(5)
  config_copper_box             Int      @default(5)
  config_pet_drill              Int      @default(10)
  config_pet_attribute_range    Int      @default(2)
  config_pet_drill_experience   Int      @default(100)
  config_pet_hour               Int      @default(20)
  config_pet_drill_odds         Int      @default(90)
  config_dog_food_exp           Int      @default(1)
  config_dog_food_life          Int      @default(10)
  config_dog_high_food_exp      Int      @default(1)
  config_dog_high_food_life     Int      @default(10)
  config_auto_harvest_time      Int      @default(1)
  config_auto_seed_time         Int      @default(1)
  config_luck_active_pro        String?   @default("10")
  config_luck_add_pro           String?   @default("10")
  config_dog_food_rose_exp      String?   @default("0")
  config_dog_high_food_rose_exp String?   @default("0")
  config_pet_drill_rose_exp     String?   @default("10")
  config_special_exp            String?   @default("1000")
  config_pet_food_attribute     String?   @default("6")
  config_diamond_first          String?   @default("0")
  config_diamond_second         String?   @default("200")
  config_diamond_third          String?   @default("2000")
  config_diamond_give           String?   @default("25")
  config_diamond_recharge       String?   @default("1")
  config_diamond_one            String?   @default("1")
  config_diamond_two            String?   @default("0")
  config_diamond_three          String?   @default("0")
  config_rmb_gold               String?   @default("1")
  config_open_time              String?   @default("09:00:00")
  config_close_time             String?   @default("23:00:00")
  config_limit_up               String?   @default("50")
  config_limit_down             String?   @default("50")
  config_trade_cost             String?   @default("0.01")
  config_set_open_price         String?   @default("0.01")
  config_seed_open_price        String?   @default("0.01")

  @@map("think_game_config")
}

// ==================== 宠物列表 ====================
model PetList {
  id              Int      @id @default(autoincrement())
  pet_name        String?  
  pet_score       String?  
  pet_identifier  String?  
  pet_attack      String?  
  pet_defense     String?  
  pet_speed       String?  
  pet_luck        String?  
  pet_life        String?  
  pet_description String?  

  @@map("think_pet_list")
}

// ==================== 房屋列表 ====================
model HouseList {
  id               Int      @id @default(autoincrement())
  house_name       String?  
  house_level      String?  
  house_info       String?  
  house_description String? 

  @@map("think_house_list")
}

// ==================== 游戏记录 ====================
model GameRecord {
  id            Int       @id @default(autoincrement())
  userid        Int?
  username      String?   
  game_time     DateTime? 
  recharge      Int?
  recharge_money Int?
  fc            Int?

  @@index([userid])
  @@map("think_game_record")
}

// ==================== 商店分类 ====================
model ShopClassList {
  id        Int      @id @default(autoincrement())
  name      String?  
  sort      Int      @default(0)
  status    Int      @default(1)

  @@map("think_shop_class_list")
}

// ==================== 商店商品 ====================
model ShopCommodityList {
  id            Int       @id @default(autoincrement())
  name          String?   
  classid       Int?
  price         Int?
  originalPrice Int?
  inventory     Int?
  sales         Int?
  pic           String?   
  status        Int       @default(1)
  description   String?   
  sort          Int       @default(0)

  @@map("think_shop_commodity_list")
}

// ==================== 签到记录 ====================
model SignRecord {
  id        Int       @id @default(autoincrement())
  userid    Int?
  username  String?   
  time      DateTime? 
  type      Int       @default(1)

  @@map("think_sign_record")
}

// ==================== 文章分类 ====================
model ArticleClass {
  id        Int      @id @default(autoincrement())
  classname String   @unique 

  @@map("think_articleclass")
}

// ==================== 文章表 ====================
model Article {
  id            Int       @id @default(autoincrement())
  author        String    
  releasetime   Int
  modifiedtime  Int
  title         String    
  content       String    
  browsenum     Int
  classid       Int

  @@map("think_article")
}

// ==================== 系统配置 ====================
model Sys {
  id        Int      @id @default(autoincrement())
  name      String?  
  value     String?  

  @@map("think_sys")
}

// ==================== 系统信息 ====================
model SysInfo {
  id        Int      @id @default(autoincrement())
  name      String?  
  value     String?  

  @@map("think_sysinfo")
}

// ==================== 公告表 ====================
model Notice {
  id        Int       @id @default(autoincrement())
  title     String?   
  content   String?   
  time      DateTime? 
  status    Int       @default(1)

  @@map("think_notice")
}

// ==================== 地块等级列表 ====================
model LandClassList {
  id              Int      @id @default(autoincrement())
  landclass_name  String?     // 地块名称（如：戈壁滩、盐碱地、胶泥地、金沙地）
  landclass_level String?     // 地块等级（1-4）
  landclass_info  String?     // 升级所需材料 JSON（如：{"crops":"9:225;10:225","diamond":"300"}）
  landclass_crops String?     // 可种植作物列表
  land_img        String?     // 地块图片

  @@map("think_land_class_list")
}
